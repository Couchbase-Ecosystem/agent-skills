{
  "skill": "couchbase-natural-language-querying",
  "cases": [
    {
      "name": "simple count with correct keyspace",
      "input": "How many airports are in France? Use the travel-sample bucket.",
      "expect": ["SELECT", "COUNT", "WHERE", "travel-sample"],
      "reject": ["$match", "db.", "aggregate(", ".find(", "$group", "`travel-sample`.inventory"],
      "threshold": 3,
      "tier": 3,
      "smoke": true,
      "expect_skill": "couchbase-natural-language-querying",
      "expect_tools": ["run_sql_plus_plus_query"]
    },
    {
      "name": "analytical opener triggers aggregation",
      "input": "What was the average number of stops across routes in travel-sample?",
      "expect": ["AVG"],
      "reject": ["$avg", "db.", ".aggregate(", "`travel-sample`.inventory"],
      "threshold": 1,
      "tier": 3
    },
    {
      "name": "aggregation shape",
      "input": "Which airlines operate the most routes in travel-sample? Top 5.",
      "expect": ["GROUP BY", "COUNT", "ORDER BY"],
      "reject": ["$group", "$sort", "db.", "aggregation pipeline", "`travel-sample`.inventory"],
      "threshold": 2,
      "tier": 3,
      "scenario": true,
      "expect_skill": "couchbase-natural-language-querying",
      "expect_tools": ["run_sql_plus_plus_query"]
    },
    {
      "name": "join routes to airline names",
      "input": "For travel-sample, list a few routes along with the name of the airline operating each.",
      "expect": ["SELECT", "JOIN", "airline"],
      "reject": ["$lookup", "db.", ".aggregate(", "`travel-sample`.inventory"],
      "threshold": 2,
      "tier": 3,
      "scenario": true,
      "expect_skill": "couchbase-natural-language-querying",
      "expect_tools": ["run_sql_plus_plus_query"]
    },
    {
      "name": "array handling with UNNEST",
      "input": "What are the scheduled flight days for routes out of SFO in travel-sample?",
      "expect": ["UNNEST", "WHERE"],
      "reject": ["$unwind", "db.", "`travel-sample`.inventory"],
      "threshold": 1,
      "tier": 3
    },
    {
      "name": "grounding: corrects a nonexistent field",
      "input": "In travel-sample, list airports by their airport_name field.",
      "expect": ["airportname"],
      "reject": ["$match", "db.", "`travel-sample`.inventory"],
      "threshold": 1,
      "tier": 3
    },
    {
      "name": "boundary: semantic search defers to the Search Service",
      "input": "Find hotels similar to 'a cozy romantic beachfront getaway'.",
      "expect": ["Search Service"],
      "reject": ["LIKE", "REGEXP"],
      "threshold": 1,
      "tier": 3
    },
    {
      "name": "boundary: optimization defers to optimizer skill",
      "input": "This SQL++ query against travel-sample is slow — make it faster.",
      "expect": ["couchbase-query-optimizer"],
      "reject": ["$match"],
      "threshold": 1,
      "tier": 3
    },
    {
      "name": "boundary: refuses writes (read-only)",
      "input": "Delete all routes in travel-sample that have zero stops.",
      "expect": ["read-only"],
      "reject": [],
      "threshold": 1
    },
    {
      "name": "generate-only on request",
      "input": "Just give me the SQL++ to count hotels in Paris in travel-sample — don't run it.",
      "expect": ["SELECT", "COUNT"],
      "reject": ["$match", "db.", ".find(", "`travel-sample`.inventory"],
      "threshold": 1,
      "tier": 3
    },
    {
      "name": "retrieval verb + geo radius triggers the skill",
      "input": "Fetch the list of hotels around Los Angeles within five miles radius using Couchbase data.",
      "expect": ["SELECT"],
      "reject": ["$match", "db.", ".find(", "`travel-sample`.inventory"],
      "threshold": 1,
      "tier": 3,
      "expect_skill": "couchbase-natural-language-querying",
      "expect_tools": ["run_sql_plus_plus_query"]
    },
    {
      "name": "retrieval verb 'fetch' triggers the skill",
      "input": "Fetch all airlines based in the United States from travel-sample.",
      "expect": ["SELECT", "WHERE"],
      "reject": ["$match", "db.", "`travel-sample`.inventory"],
      "threshold": 1,
      "tier": 3,
      "expect_skill": "couchbase-natural-language-querying",
      "expect_tools": ["run_sql_plus_plus_query"]
    }
  ]
}
